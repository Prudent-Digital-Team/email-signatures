# email-signatures
Email Signatures for all Prudent Groups Domains

## Format
The Ionos `email signatures` does not allow bootstraping
and CSS2+, this impacts the display across other email 
servers (Gmail | Hotmail). 

Safe pratice will be to use tables as the framework and 
attach the contents withint the table
